<ul>
  <% @tasks.each do |task| %>
    <li><%= check_box("task", task.id, {checked: task.completed})%> <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to "Add a new task", tasks_new_path %>
